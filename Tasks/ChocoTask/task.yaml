name: choco
command: "./choco.ps1 -Package {{package}} {{#if ignoreChecksums}}--ignore-checksums{{/if}}"
inputs:
  package:
    defaultValue: ""
    type: "string"
    required: true
  ignoreChecksums:
    defaultValue: false
    type: "boolean"
    required: false
